{
  "entities": {
    "ChatConversation": {
      "title": "Chat Conversation",
      "description": "A conversation with the doctrinal assistant.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "lastMessage": { "type": "string" }
      },
      "required": ["userId", "title", "createdAt", "updatedAt"]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "description": "A message within a doctrinal chat conversation.",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "assistant"] },
        "content": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["role", "content", "timestamp"]
    },
    "UserProgress": {
      "title": "User Progress",
      "description": "Progress of a user in the courses.",
      "type": "object",
      "properties": {
        "completedLessons": {
          "type": "map",
          "description": "Map of lesson ID to lesson progress data"
        },
        "completedBlockExams": {
          "type": "map",
          "description": "Map of block exam ID to exam progress data"
        }
      }
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "User profile settings.",
      "type": "object",
      "properties": {
        "fullName": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "email": { "type": "string" },
        "studyReminderEnabled": { "type": "boolean" },
        "studyReminderTime": { "type": "string" }
      }
    }
  },
  "firestore": {
    "conversations/{conversationId}": {
      "schema": { "$ref": "#/entities/ChatConversation" },
      "description": "Top-level metadata for assistant conversations."
    },
    "conversations/{conversationId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/ChatMessage" },
      "description": "Messages belonging to a specific conversation."
    },
    "users/{userId}/progress/default": {
      "schema": {
        "$ref": "#/entities/UserProgress"
      },
      "description": "Progress data for a specific user. Using 'default' as the document ID for the single progress record."
    },
    "users/{userId}/profile/default": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Profile data for a specific user."
    }
  }
}
